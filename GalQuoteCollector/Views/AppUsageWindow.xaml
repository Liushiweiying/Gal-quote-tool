<Window x:Class="GalQuoteCollector.Views.AppUsageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="应用使用时长"
        Width="620" Height="700"
        MinWidth="460" MinHeight="440"
        WindowStartupLocation="CenterOwner"
        Background="#F2F2F7" FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="SegToggle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#3C3C43"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="#1D1D1F"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16,12,16,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 返回 + 应用名 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Click="OnBack" Padding="10,5" FontSize="13" Cursor="Hand"
                    Background="#E4E4EA" Foreground="#3C3C43" BorderThickness="0"
                    Content="← 返回" ToolTip="回到使用时间"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Border x:Name="IconHost" Width="30" Height="30" CornerRadius="7"
                        Background="#5B6ABF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock x:Name="AppNameText" FontSize="20" FontWeight="Bold" Foreground="#1D1D1F"
                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Grid>

        <!-- 时间段 -->
        <Border Grid.Row="1" Background="#E4E4EA" CornerRadius="9" Padding="2"
                HorizontalAlignment="Left" Margin="0,10,0,10">
            <StackPanel Orientation="Horizontal">
                <ToggleButton x:Name="DayBtn" Content="今天" Click="OnDay" Style="{StaticResource SegToggle}" Padding="14,4" MinWidth="62"/>
                <ToggleButton x:Name="WeekBtn" Content="最近 7 天" Click="OnWeek" Style="{StaticResource SegToggle}" Padding="14,4" MinWidth="78"/>
                <ToggleButton x:Name="MonthBtn" Content="本月" Click="OnMonth" Style="{StaticResource SegToggle}" Padding="14,4" MinWidth="62"/>
                <ToggleButton x:Name="YearBtn" Content="今年" Click="OnYear" Style="{StaticResource SegToggle}" Padding="14,4" MinWidth="62"/>
                <ToggleButton x:Name="CustomBtn" Content="自选" Click="OnCustom" Style="{StaticResource SegToggle}" Padding="14,4" MinWidth="62"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="Panel"/>
        </ScrollViewer>
    </Grid>
</Window>
