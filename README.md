# Gal 语录收藏工具

[English](README.en.md) | [日本語](README.ja.md)

![Platform](https://img.shields.io/badge/platform-Windows-lightgrey)
![.NET](https://img.shields.io/badge/.NET-8.0-purple)

一键收藏 galgame 台词。截图 + OCR 自动识别 + 标签分组 + 回想幻灯片。

![主窗口](images/main.png)

## 功能

| 功能 | 说明 |
|---|---|
| **一键采集** | 全局热键 `Ctrl+Win+Z`（可自定义），截图 → OCR → 自动保存；OCR 可选 Windows 内置 / Ollama 本地模型 / RapidOCR 三种引擎 |
| **游戏名识别** | 自动剥离引擎/日期后缀，支持自定义匹配规则 |
| **标签** | 给语录打标签，按标签筛选 |
| **分组** | 创建分组合集，一条语录可属于多个分组 |
| **回想幻灯** | 全屏模式，键盘翻页，随机跳转，置顶 |
| **导出** | Markdown / JSON，全部或按分组 |
| **导入** | 导入已导出的 Markdown / JSON |
| **统计** | 按游戏 / 标签 / 分组 / 时间统计 |
| **开机自启** | 随 Windows 启动，最小化到托盘 |
| **采集延迟** | 0~2000ms 可调 |

## 下载

| 文件 | 大小 | 说明 |
|---|---|---|
| `Gal-quote-tool.exe` | ~28MB | 单文件，需 .NET 8 运行时 |
| `Gal-quote-tool_selfcontained.exe` | ~190MB | 单文件自包含，无需运行时 |
| `Gal-quote-tool_Setup.exe` | ~55MB | Inno Setup 安装包（自包含，含卸载程序） |
| `publish-folder.zip` | ~77MB | 自包含压缩包，解压即用 |

> 数据目录：`%LOCALAPPDATA%\GalQuoteCollector\`

## 使用

1. 运行 exe，自动缩小到系统托盘
2. 打开 galgame，按 `Ctrl+Win+Z` 采集
3. 右上角弹出通知（点击打开主窗口）

### 快捷键

| 按键 | 回想 / 全屏中 |
|---|---|
| `Ctrl+Win+Z` | 采集（可自定义） |
| `←` / `→` | 上一条 / 下一条 |
| `Space` | 下一条 |
| `Enter` | 随机跳转 |
| `F11` | 全屏切换 |
| `F2` | 置顶切换 |
| `Esc` | 退出全屏 / 关闭 |

### 分组
- 详情面板输入分组名 → 创建
- 点击灰色标签 → 加入（变绿）
- 点击绿色标签 → 移出
- 右键标签 → 删除分组
- 工具栏选分组 → 筛选

### 游戏名匹配规则
设置中添加规则。如 `Summer Pockets → Summer Pockets REFLECTION BLUE`，标题含 "Summer Pockets" 的窗口自动归到该游戏名下。

## 开发

### 环境
- [.NET 8 SDK](https://dotnet.microsoft.com/download/dotnet/8.0)
- Windows 10+（中文 OCR 语言包）

### 构建
```bash
cd GalQuoteCollector
dotnet build
dotnet run
```

### 发布
```bash
# 框架依赖（小）
dotnet publish -r win-x64 -c Release --self-contained false -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o ./publish

# 自包含（便携）
dotnet publish -r win-x64 -c Release --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -o ./publish
```

## 技术栈
- **.NET 8 + WPF** — 桌面框架
- **Windows.Media.Ocr** — 原生 OCR（可切换 Ollama 本地模型 / RapidOCR）
- **SQLite** — 本地数据库
- **CommunityToolkit.Mvvm** — MVVM
- **Hardcodet.NotifyIcon.Wpf** — 系统托盘
- **System.Drawing.Common** — 截图与图像处理

## 数据目录
```
%LOCALAPPDATA%\GalQuoteCollector\
├── quotes.db
├── settings.json
└── screenshots\
```

## 更新日志

### v1.3.6（2026-09-26）· beta
- **新增 网页「访问协议」三选一**：自动（同一端口 `http://` 与 `https://` 都能连，推荐）/ 仅 HTTP / 仅 HTTPS。以前 tunnel 或反向代理的 origin 协议和程序对不上就会报 `SSL handshake failed` / 502，现在两种都能应答；自签证书改成「真收到 TLS 连接才生成」
- **新增「接收测试版（beta）」开关**（设置 → 常规，默认关）：打开后检查更新会把预发布也算进来，想第一时间试新功能就开；关着只推正式版。预发布构建自报版本号带 `-beta`，所以**正式版发布时 beta 用户也会收到提醒**
- **新增 局域网 / 公网两把独立的锁**（都默认关，最安全）：`允许局域网访问` 只管监听范围（关掉时 tunnel 照样能用，走本机回环）；`允许公网访问` 只管来源判断（未开启时 tunnel / 反代进来的请求一律 403）。可以只给内网、只给公网、或都给
- **新增 公网两步验证（TOTP）**：用手机验证器 App（Microsoft / Google Authenticator、Aegis 等）的 6 位动态码，**只对公网生效**，局域网免验证；通过后 12 小时内免输（可调）；连续错 8 次自动封锁 10 分钟；设置里有生成密钥 / 显示二维码 / 复制 otpauth 链接，还会显示当前动态码方便核对手机配对成功没有
- **新增 二维码插件**：二维码改为可选插件（`plugins\GalQuoteCollector.Plugins.Qr.dll` + `QRCoder.dll`），主程序不依赖任何二维码库；没装插件也能用「手动输入密钥」
- **新增 电视 / 大屏模式**：电视盒子、投影、客厅大屏打开网页自动切大字号，**遥控器方向键 + OK 键即可操作**（回想里上下左右翻页、OK 开关自动播放）；设置里会给出 `?tv=1` 与 `?tv=1&view=1&auto=1` 两个可直接收藏的地址
- **新增 网页只读模式**：可强制整个网页只读（连局域网也不能改），配合单向同步即可当「只读镜像」
- **新增 无界面版（beta）**：同一套网页可以脱离 Windows 跑在 Linux / Armbian 电视盒子 / NAS 上（`galquote-server`），电脑关机后手机和电视照样能看；带 `--data / --port / --code / --totp / --read-only` 等命令行选项，附 systemd 服务与一键安装脚本
- **修复** 网页判断「是不是局域网」改为按请求来源，之前用「能不能改」代替，强制只读时会把局域网误报成外网
- **说明** 这一版是 **beta**：功能都在真机上验过（访问码 / 动态码登录、只读、大屏模式、只读镜像都实测通过），但正式版之前可能还有小调整；**自动更新只推正式版**，beta 需要手动下载

### v1.3.5（2026-09-23）
- **改进** 退出全屏后 Magpie 又超分一下：不再去判断它是否已自行结束，**退出时直接补发一次超分热键**把状态收干净（闪现从约 1.5 秒压到 0.4 秒）；之后 2 秒内若又冒头再收一次（最多两次）
- **修复** 设置窗口左侧分类跳转滚过头（标题被顶出可视区）：改回框架自带的 BringIntoView，再往上留 6px

### v1.3.4（2026-09-21）
- **改进 自动备份规则**（按你的要求）：备份放在**安装目录上一级**的 `Gal Quote Tool Backup`；每天首次启动时**只有语录库内容有变动**才备份（用内容哈希判断，避免 SQLite 每次打开刷新时间戳导致天天都备份）；**最多保留份数可在设置里调**（默认 3）；**备份不含设置，设置变动也不触发备份**
- **新增 设置窗口左侧分类导航**：快捷键 / 常规 / 回想·超分 / 截图与黑边 / 数据与备份 / 网页与手机 / OCR 识别 / 游戏名规则，点击直接跳到对应分组（精确偏移滚动）
- **新增 回想专属设置按钮（⚙，纯图标）**：黑边处理、**文字底样式（黑底白字 / 白底黑字）+ 透明度（可恢复默认）**、循环、中英文字体、Magpie 超分与热键；应用后立即生效并写回设置
- **新增 单条语录 ZIP 导出/导入**：桌面右键「导出这条（含截图 ZIP）」、网页卡片「导出ZIP」、网页「导入」支持 .json 与 .zip；包内是 `quotes.json` + `screenshots/`，与「打包导出/导入」完全兼容
- **修复** 退出全屏后 Magpie 又超分一下：不再补发热键（Magpie 自己会结束），并加了 200ms 轮询的收尾，把它自己又冒头的那次立刻收掉
- **修复** 回想顶栏 ⚙ 按钮被窗口右侧裁掉半边（移进自适应列，状态文字改为省略号截断）

### v1.3.3（2026-09-21）
- **新增 内网网页（手机 / 电脑浏览器）**：程序内起一个网页服务（可选 HTTPS，见下），同一 WiFi 下打开 `http://电脑IP:8088/` 就能**查看、搜索、修改、导入导出**语录；页面自适应手机与电脑
  - **回想模式**：点「回想」或点卡片缩略图进入全屏看图，左右滑动/点击切换、自动播放、显示游戏名与正文
  - **黑边处理（不修改文件）**：回想里可一键切换「原样 / 裁掉黑边 / 黑边涂白」，由服务端在内存里处理，原图不动
  - **单条导入导出（含图片）**：每张卡片可导出单条 JSON（内嵌 base64 图片），头部「导入」按钮选文件即可入库
  - **筛选反选**：游戏 / 分组 / 标签各自可「排除」；未识别到文字的条目只显示截图，不再显示「[未识别到文字]」这几个字
  - **HTTPS**：默认开启（自签证书，首次自动生成）；设置里可「在本机信任（消除浏览器警告）」，手机可「导出证书」安装
- **新增 每日首次启动自动备份**：备份语录库 / 使用记录 / 设置 + **截图**，每天一次、只保留最近 3 天，目录可自定义
- **新增 截图黑边处理**：截图时自动裁掉四周纯黑边（可在设置里关）；主界面「···」→「裁掉截图黑边（全部）」批量处理已有截图；画面偏暗会自动跳过
- **改进 回想（Magpie 超分）**：改用一个热键到位——Magpie 在跑就直接发超分热键（若原本在超分别的窗口会自动补一次切过来）；不再依赖 Magpie 日志（它是缓冲写盘的，之前会误判导致"要点两次"）
- **改进 回想窗口**：顶栏/按 `B` 键切换「原样 / 裁掉黑边 / 黑边涂白」三种显示模式（**不改文件**），全屏时背景跟着变白
- **修复** 应用详情页「平均每天」的分母改为**有记录的天数**（之前用整个区间天数，数字被稀释）
### v1.3.2（2026-09-19）
- **修复** 锁屏时长被严重高估（v1.3.0/v1.3.1 的问题）
  - 原因：旧逻辑把「输入桌面不是 Default」直接当成锁屏，实测在**没有真实锁屏事件**的情况下也会成立，把正在玩游戏的时间整晚记成了锁屏（一天 229 分钟里约 145 分钟是误判）
  - 现在按可靠度三重判定：① **真正的会话锁定事件**（`SessionLock`/`SessionUnlock`；`ConsoleDisconnect`/`RemoteDisconnect` 不再算锁屏）② **前台是锁屏程序**（`LockApp.exe` / `LogonUI.exe`，可自定义）③ 输入桌面非 Default **且已 ≥2 分钟没有任何输入**（`GetLastInputInfo`）
  - 每次记锁屏都会在 `startup.log` 写明原因；若解锁事件丢失，检测到桌面回到 Default 会自动恢复
- **新增** 截图热键不再传给游戏（设置里可关）：很多引擎自带 `Alt+E` 之类热键，之前按截图热键会连带弹出游戏自己的窗口，现在默认吞掉这次按键（在本程序自己的窗口里不拦截）
- **修复** 在设置窗口里改热键时不再误触发一次截图（改键期间挂起热键）
- **新增** 历史数据修正开关（命令行，一般用不到）：`--fix-lock <日期> <进程名> [起小时 止小时]` 把某天误记的锁屏时长改记到指定应用、`--move-usage <日期> <源进程> <目标进程> [起小时 止小时]` 把某天的时长从一个应用改记到另一个
### v1.3.1（2026-09-16）
- **新增** 点击应用行进入**该应用的详情页**（对应 iOS 里点进某个 App 的屏幕使用时间页）
  - 大字「平均每天 X」+「N 天合计 Y」+ **该应用自己的柱状图**（今天按小时、7 天/本月按天、今年按月）
  - 统计范围：区间 / 合计 / 平均每天 / 有记录的天数 / 最多的一天 / 最近使用 / **占同期总时长百分比** / 进程名
  - 应用行右侧加了 `›` 箭头提示可点；详情页自带时间段切换（今天 / 7 天 / 本月 / 今年 / 自选），`Esc` 或「← 返回」关闭
- **修复** 应用行之前只有文字本身能点到（名称和时长之间的空白点了没反应）→ 现在整行都可点
### v1.3.0（2026-09-16）
- **重做**「使用时间」页：iOS「屏幕使用时间」式卡片布局
  - 顶部大数字总时长 + 与上一区间对比 + **24 小时 / 按天 / 按周 / 按月柱状图**；鼠标划过柱子弹出数值气泡（点击钉住，Esc 或点空白取消），其余柱子自动变淡
  - 时间刻度按窗口宽度自适应（窄窗口不截断），图表高度随宽度变化，宽屏时应用列表自动两列，窗口尺寸记忆
- **新增** 锁屏时间单独统计（绿色）
  - 锁屏期间只记「锁屏」，不再算到游戏/软件头上；`LockApp.exe`（Windows 默认锁屏界面）、`LogonUI.exe` 等锁屏进程自动归入锁屏，**历史数据一次性迁移**
  - 柱状图下蓝（使用）上绿（锁屏）堆叠，卡片副标题与统计范围都有绿色锁屏时长
  - 判断方式：系统会话事件 + 输入桌面检测（壁纸软件画的锁屏也能识别）+ 可自定义的锁屏进程列表
- **新增** 时间段：今天 / 最近 7 天 / 本月 / 今年 / **自选（日历选起止日期）**，并自动和上一个等长区间对比
- **新增**「应用名 / 锁屏进程」窗口：进程名 → 显示名自定义（每行可单独保存/复原，列表支持按时间或按名称排序）
  - 同一应用的不同进程名、大小写不再被拆成多条（按进程名归组；明显是窗口标题的名字自动退回进程名）
- **新增** 应用图标多来源自动解析（记录路径 → 运行中进程 → MuiCache → 卸载信息 → App Paths → Everything HTTP），取不到用首字母色块；右键某一行可自定义图标（选图片 / 选 exe / 自动搜索 exe / 恢复自动）
- **新增** 应用行显示时长进度条，可展开「显示更多」查看全部
- **改进** 所有窗口的滚轮/触摸板滚动改为**半速平滑滚动**
- **改进** 黑名单窗口重做：直接列出已记录的应用，点一下就加入黑名单，并询问是否同时删除已记录的数据
- **移除** 开机自启时把进程设成 High 优先级（常驻进程占 High 会跟游戏抢 CPU）
### v1.2.6（2026-09-13）
- **修复** 回想超分作用在**错误的窗口**上：之前打开回想窗口就触发超分，超分的是那个小窗口；现在改为**按 F11 进入全屏时才超分，超分的就是全屏的那个窗口**，退出全屏/关闭回想时自动解除
- **修复** 用日志确认超分是否真的生效：Magpie 的热键是开关语义，**正在缩放别的窗口时按下去只会停掉它**——现在会自动补发一次把超分切到全屏回想上，状态栏也会写明「原窗口的超分已停止」
- **修复** 退出全屏时不再盲目补发热键（之前会把刚结束的会话又开起来，超分跑到小窗口上）：先看 Magpie 日志，它自己已经结束了就不动
- **修正** v1.2.5 里「Magpie 以管理员身份运行会导致超分失效」的说法是错的——实测管理员权限下热键注入同样有效
- **新增** 诊断开关 `--open-slideshow`：打开回想（强制开启超分）并自动进全屏→超分→退出全屏，便于验证这条链路

### v1.2.5（2026-09-13）
- **修复** TranslucentTB 开机修复失效：以前开机登录后 1 秒就重启 TTB，此时 shell 还没就绪，重启等于白做。现在改为**等任务栏通知区出现 → 再等 20 秒 → 重启**，再用任务栏像素校验是否生效，没生效会在 30 秒后**自动重试一次**
- **变更** 截图方式默认改为**当前显示器**：Magpie 超分不会让游戏顶栏消失，按窗口截取会把标题栏一起截进去
  - 老配置会**自动迁移一次**（`auto`／空值 → 当前显示器），迁移后仍可自由改回
  - 「检测到 Magpie 优先抓原生分辨率」默认关闭（开着会带上标题栏），需要原生分辨率时可在设置里打开
- **新增** 回想模式支持 **Magpie 超分**：按 **F11 进入全屏**后自动触发 Magpie 缩放该全屏窗口，退出全屏自动解除
  - 热键**自动读取 Magpie 配置**（也可手动填写），Magpie 未运行时会尝试自动启动
  - 设置里有「读取 Magpie 配置」与「测试」按钮：测试会发一次热键，并查 Magpie 日志确认是否真的生效
  - 若 Magpie 正在缩放游戏，会把超分切换到这个全屏窗口（游戏那边的超分会停止）
- **新增** 回想窗口图片改用高质量缩放算法

### v1.2.4（2026-09-12）
- **新增** 更新器按**当前部署形态**升级（继承原有安装方式）：
  - 安装版（Setup 装的）→ 下载 Setup 原地升级，沿用原安装目录，语录/截图/设置不动
  - 单文件版 → 下载同名 exe，退出后**替换自身并重启**
  - 文件夹版 → 下载 publish-folder.zip，退出后**覆盖当前目录并重启**
  - 更新窗口会显示识别到的「升级方式」与将要下载的文件
- **修复** 升级前由辅助脚本**等待本程序完全退出**再动作，避免文件占用导致的安装失败

### v1.2.3（2026-09-12）
- **修复** TranslucentTB 修复功能之前无效：Store 版无法用 exe 路径启动，现在改为**真正重启**（读取 AUMID 激活，兼容传统安装），并校验进程是否恢复
- **新增** 设置中「立即修复一次」按钮，无需重启即可测试该修复
- **新增** TranslucentTB 选项常显（之前 TTB 未运行时会隐藏），全过程写入 startup.log
- **新增** 检测到 Magpie 运行时，自动模式优先抓取**游戏原生分辨率**（PrintWindow），可在设置中关闭
- **新增** 采集/补拍后在状态栏与提示中显示**实际分辨率与截图方式**（如 `1280×720 窗口内容（原生）`）

### v1.2.2（2026-09-12）
- **新增** 截图方式可选：自动 / 窗口内容（原生分辨率）/ 窗口可见区域 / 当前显示器 / 整个屏幕——串流窗口、特殊引擎（如 minori）截图不全时可切换
- **修复** 截图改用 DWM 真实窗口边界，窗口被缩放、部分离屏或尺寸异常时不再截偏/截半
- **修复** 复制失败（剪贴板被其他程序占用）：改用剪贴板重试机制，短暂占用不再报错
- **修复** 时长统计柱状图改为绝对值刻度（满格刻度自适应显示），不再恒定满格
- **新增** 日历中高亮标记有使用记录的日子

### v1.2.1（2026-08-13）
- **新增** RapidOCR 本地离线 OCR 引擎（设置 → OCR 引擎，需装有 rapidocr-onnxruntime 的 Python）
- **新增** 撤销删除：删除语录后可在工具栏一键恢复（保留最近 20 条，含标签/分组/截图关联）
- **新增** 采集后自动选中新语录
- **新增** 主窗口位置/大小记忆（下次启动自动恢复）
- **新增** 一键复制语录文本（详情区按钮 / 列表右键菜单）
- **新增** 拖拽 .md/.json/.zip 文件到窗口直接导入
- **新增** 搜索防抖与多关键词（空格分隔，需全部命中）
- **新增** Toast 通知淡入淡出与多通知堆叠
- **新增** 截图右键「打开所在文件夹」
- **新增** 设置窗口「恢复默认」与「查看日志」
- **修复** 卸载器删除数据时移入回收站（可恢复，不再永久删除）
- **修复** SQLite 多线程并发访问竞态（后台加载与热键采集同时进行时可能报错）
- **修复** 热键显示错误（如 F5 显示为 "t"）
- **修复** 采集热键与补拍热键相同时无提示
- **修复** 导入压缩包可被路径穿越、静默覆盖本地设置与使用记录（现在会先确认）
- **修复** 多显示器下全屏截图只截主屏、副屏负坐标截图错误
- **修复** 窗口化游戏被强制整屏截图（改为窗口覆盖屏幕 ≥90% 才全屏）
- **修复** 大量语录时标签/分组筛选卡顿（消除逐条查询）
- **修复** 批量删除不询问截图处理（与单条删除一致）
- **修复** 采集失败/未检测到窗口时主窗口保持最小化
- **修复** 采集延迟 0ms 时可能截到自身窗口（强制 ≥100ms）
- **优化** 启动日志与 OCR 缓存设上限；版本号统一从程序集读取
