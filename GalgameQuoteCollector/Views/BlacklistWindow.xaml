<Window x:Class="GalgameQuoteCollector.Views.BlacklistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="管理黑名单"
        Width="360" Height="300"
        WindowStartupLocation="CenterOwner"
        Background="#FFF" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="黑名单中的进程不会被记录使用时长" FontSize="12" Foreground="#888" Margin="0,0,0,8"/>

        <ListBox Grid.Row="1" x:Name="BlacklistBox" Margin="0,0,0,8"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="ProcessInput" Padding="6,4" VerticalContentAlignment="Center"/>
            <Button Grid.Column="1" Content="添加" Click="OnAdd" Padding="10,4" Margin="4,0,0,0" Background="#5B6ABF" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            <Button Grid.Column="2" Content="关闭" Click="OnClose" Padding="10,4" Margin="4,0,0,0" Background="#EEE" Foreground="#333" BorderThickness="0" Cursor="Hand"/>
        </Grid>
    </Grid>
</Window>
